[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "loro-agent"
version = "0.1.1"
description = "Enterprise memory-sharing CLI agent harness for coding, governed data, and productivity work."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Alex Merced" }]
keywords = ["agents", "cli", "enterprise", "memory", "iceberg", "polaris"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Intended Audience :: Information Technology",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Office/Business",
  "Topic :: Software Development",
  "Topic :: System :: Shells",
]
dependencies = [
  "typer>=0.12.5",
  "rich>=13.9.0",
  "pydantic>=2.9.0",
  "pydantic-settings>=2.5.0",
  "tomli-w>=1.0.0",
  "httpx>=0.27.0",
  "structlog>=24.4.0",
  "python-docx>=1.1.2",
  "python-pptx>=1.0.2",
  "openpyxl>=3.1.5",
  "xlsxwriter>=3.2.0",
]

[project.optional-dependencies]
artifacts = [
  "pandas>=2.2.0",
]
data = [
  "sqlalchemy>=2.0.35",
  "psycopg[binary]>=3.2.0",
  "pyiceberg>=0.8.0",
  "pyarrow>=17.0.0",
]
aws = [
  "boto3>=1.34.0",
]
integration = [
  "psycopg[binary]>=3.2.0",
  "testcontainers[postgres]>=4.8.0",
]
dev = [
  "coverage[toml]>=7.6.0",
  "pytest>=8.3.0",
  "pytest-asyncio>=0.24.0",
  "pytest-cov>=5.0.0",
  "ruff>=0.6.9",
]

[project.scripts]
loro = "loro.cli:app"

[project.urls]
Homepage = "https://github.com/alexmerced-oss/loro"
Documentation = "https://github.com/alexmerced-oss/loro/tree/main/docs"
Repository = "https://github.com/alexmerced-oss/loro"
Issues = "https://github.com/alexmerced-oss/loro/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/loro"]

[tool.hatch.build.targets.sdist]
exclude = [
  "/.loro",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "integration: tests that require local containers or external services",
]

[tool.coverage.run]
branch = true
source = ["loro"]

[tool.coverage.report]
show_missing = true
skip_covered = true
fail_under = 70
